->İlk Olarak GIT GUI aç(Yeni Bir repo aç)

->Git Bash çalıştır(Git GUI içerisinden)

->git init

->git status(Bulunduğun dizinde herhangi bir değişiklik yap)

->git add .

->git status

->git commit -m "yeni degisiklik var"

->git push -u origin master

--Ve kodu göndermek istediğin adres için git bilgilerini gir: 
->git remote add origin https://github.com/AhmetDilli31/GitKomutlari.git(Github ta Oluşturduğun yeni proje adresi ne ise buraya eklemen gerek!)
->git config --global user.email "ahmetdilliblgsyr@gmail.com"
->git config --global user.name "AhmetDilli31"
Deneme
